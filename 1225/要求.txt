关于注册登陆发布文章系统

写一个注册登陆的功能页面(index.html)，在登陆成功后跳转到一个新的页面(content.html)，这个页面中有一个发布文章的功能，具体布局为:

1).有两个input框 一个为文章题目，一个为文章内容，还有一个为发布的button按钮，
2).在点击button按钮后，获取到input的value传到后台，在后台获取，并写入到一个为wenzhang.txt的文档中
3).再写入成功后在读取一次，将wenzhang.txt里的内容返回给前台
4).最后在前台将数据获取并渲染到页面中